Random Creation — Handoff

Written at the 2026-08-07 typo-fix close-out (AUTO) — a micro-session that corrected two
characters in CLAUDE.md's AI PM pointer block (\closeout → /closeout, \closeout-auto →
/closeout-auto), aligning it with the canonical 4.0 block. The AI PM 3.2 → 4.0 upgrade
landed earlier the same day (commit 5c1e201); nothing about the app changed.

WHERE THINGS STAND

App at v4.0, released 2026-08-02 with installer and portable downloads at
https://github.com/akcama/random-creation/releases. Repository lives in the akcama
organization; GitHub redirects the old henry-akcama address.

NEXT SESSION SHOULD TAKE UP

The post-release chores, unchanged and in the backlog's order: download-back verification of
the published v4.0 assets, then the live user's manual upgrade (must carry her data), then
the v4.0 record doc, then Releases\ retirement (explicit ruling required — git-ignored, no
safety net).
