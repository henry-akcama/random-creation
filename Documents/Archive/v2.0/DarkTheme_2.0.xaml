<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:sys="clr-namespace:System;assembly=mscorlib">

    <!-- ═══════════════════════════════════════════════════════════════════════
         RANDOM CREATION — DARK THEME
         All color and size resources for the dark UI theme.
         Reference these keys throughout the app instead of hardcoded hex values.
         ═══════════════════════════════════════════════════════════════════════ -->

    <!-- ── Backgrounds ───────────────────────────────────────────────────── -->
    <Color x:Key="BackgroundOuterColor">#1c1c1e</Color>
    <Color x:Key="BackgroundCardColor">#2a2a2e</Color>
    <Color x:Key="BackgroundTitleBarColor">#161618</Color>
    <Color x:Key="BackgroundSummaryBarColor">#131315</Color>
    <Color x:Key="BackgroundExpandedColor">#222226</Color>
    <Color x:Key="BackgroundInputColor">#1c1c1e</Color>

    <SolidColorBrush x:Key="BackgroundOuterBrush"    Color="{StaticResource BackgroundOuterColor}"/>
    <SolidColorBrush x:Key="BackgroundCardBrush"     Color="{StaticResource BackgroundCardColor}"/>
    <SolidColorBrush x:Key="BackgroundTitleBarBrush" Color="{StaticResource BackgroundTitleBarColor}"/>
    <SolidColorBrush x:Key="BackgroundSummaryBarBrush" Color="{StaticResource BackgroundSummaryBarColor}"/>
    <SolidColorBrush x:Key="BackgroundExpandedBrush" Color="{StaticResource BackgroundExpandedColor}"/>
    <SolidColorBrush x:Key="BackgroundInputBrush"    Color="{StaticResource BackgroundInputColor}"/>

    <!-- ── Borders ───────────────────────────────────────────────────────── -->
    <Color x:Key="BorderStandardColor">#3a3a3c</Color>
    <Color x:Key="BorderSelectedColor">#4a4a4e</Color>
    <Color x:Key="BorderSubtleColor">#2a2a2c</Color>

    <SolidColorBrush x:Key="BorderStandardBrush" Color="{StaticResource BorderStandardColor}"/>
    <SolidColorBrush x:Key="BorderSelectedBrush" Color="{StaticResource BorderSelectedColor}"/>
    <SolidColorBrush x:Key="BorderSubtleBrush"   Color="{StaticResource BorderSubtleColor}"/>

    <!-- ── Text ──────────────────────────────────────────────────────────── -->
    <Color x:Key="TextPrimaryColor">#e0e0e0</Color>
    <Color x:Key="TextSecondaryColor">#d0d0d8</Color>
    <Color x:Key="TextMutedColor">#a0a0a8</Color>
    <Color x:Key="TextSectionLabelColor">#c0c0c8</Color>
    <Color x:Key="TextHistoryColor">#d0d0d8</Color>

    <SolidColorBrush x:Key="TextPrimaryBrush"       Color="{StaticResource TextPrimaryColor}"/>
    <SolidColorBrush x:Key="TextSecondaryBrush"     Color="{StaticResource TextSecondaryColor}"/>
    <SolidColorBrush x:Key="TextMutedBrush"         Color="{StaticResource TextMutedColor}"/>
    <SolidColorBrush x:Key="TextSectionLabelBrush"  Color="{StaticResource TextSectionLabelColor}"/>
    <SolidColorBrush x:Key="TextHistoryBrush"       Color="{StaticResource TextHistoryColor}"/>

    <!-- ── Accent ─────────────────────────────────────────────────────────── -->
    <Color x:Key="AccentBlueColor">#0a84ff</Color>
    <Color x:Key="AccentBlueHoverColor">#1a8fff</Color>
    <Color x:Key="AccentBluePressedColor">#0070e0</Color>
    <Color x:Key="ResultOptionColor">#60aaff</Color>

    <SolidColorBrush x:Key="AccentBlueBrush"        Color="{StaticResource AccentBlueColor}"/>
    <SolidColorBrush x:Key="AccentBlueHoverBrush"   Color="{StaticResource AccentBlueHoverColor}"/>
    <SolidColorBrush x:Key="AccentBluePressedBrush" Color="{StaticResource AccentBluePressedColor}"/>
    <SolidColorBrush x:Key="ResultOptionBrush"      Color="{StaticResource ResultOptionColor}"/>

    <!-- ── Interactive hover / button backgrounds ────────────────────────── -->
    <Color x:Key="HoverDarkColor">#2f2f35</Color>
    <Color x:Key="HoverBackButtonColor">#1a2a3a</Color>
    <Color x:Key="ButtonDarkColor">#3a3a3c</Color>
    <Color x:Key="ButtonDarkHoverColor">#4a4a4e</Color>

    <SolidColorBrush x:Key="HoverDarkBrush"        Color="{StaticResource HoverDarkColor}"/>
    <SolidColorBrush x:Key="HoverBackButtonBrush"  Color="{StaticResource HoverBackButtonColor}"/>
    <SolidColorBrush x:Key="ButtonDarkBrush"       Color="{StaticResource ButtonDarkColor}"/>
    <SolidColorBrush x:Key="ButtonDarkHoverBrush"  Color="{StaticResource ButtonDarkHoverColor}"/>

    <!-- ── Nav icon foreground ────────────────────────────────────────────── -->
    <Color x:Key="NavIconColor">#a0a0a8</Color>
    <Color x:Key="NavIconHoverColor">#e0e0e0</Color>
    <SolidColorBrush x:Key="NavIconBrush"      Color="{StaticResource NavIconColor}"/>
    <SolidColorBrush x:Key="NavIconHoverBrush" Color="{StaticResource NavIconHoverColor}"/>

    <!-- ── Window controls ───────────────────────────────────────────────── -->
    <Color x:Key="WinBtnForegroundColor">#a0a0a8</Color>
    <Color x:Key="WinBtnHoverColor">#2f2f35</Color>
    <Color x:Key="WinBtnCloseHoverColor">#c42b1c</Color>
    <SolidColorBrush x:Key="WinBtnForegroundBrush" Color="{StaticResource WinBtnForegroundColor}"/>
    <SolidColorBrush x:Key="WinBtnHoverBrush"      Color="{StaticResource WinBtnHoverColor}"/>
    <SolidColorBrush x:Key="WinBtnCloseHoverBrush" Color="{StaticResource WinBtnCloseHoverColor}"/>

    <!-- ── Weight badge colors ────────────────────────────────────────────── -->
    <SolidColorBrush x:Key="WeightNormalBackgroundBrush" Color="#1e3a1e"/>
    <SolidColorBrush x:Key="WeightNormalForegroundBrush" Color="#4caf50"/>
    <SolidColorBrush x:Key="WeightLowBackgroundBrush"    Color="#3a2e10"/>
    <SolidColorBrush x:Key="WeightLowForegroundBrush"    Color="#f0a030"/>
    <SolidColorBrush x:Key="WeightRareBackgroundBrush"   Color="#3a1a1a"/>
    <SolidColorBrush x:Key="WeightRareForegroundBrush"   Color="#e05050"/>

    <!-- ── ON/OFF pill colors ─────────────────────────────────────────────── -->
    <SolidColorBrush x:Key="PillOnBackgroundBrush"  Color="#152840"/>
    <SolidColorBrush x:Key="PillOnForegroundBrush"  Color="#0a84ff"/>
    <SolidColorBrush x:Key="PillOnBorderBrush"      Color="#0a4480"/>
    <SolidColorBrush x:Key="PillOffBackgroundBrush" Color="#252528"/>
    <SolidColorBrush x:Key="PillOffForegroundBrush" Color="#555558"/>
    <SolidColorBrush x:Key="PillOffBorderBrush"     Color="#3a3a3e"/>

    <!-- ── Collections summary bar pills ─────────────────────────────────── -->
    <SolidColorBrush x:Key="SummaryPillOnBackgroundBrush"  Color="#1e3a1e"/>
    <SolidColorBrush x:Key="SummaryPillOnForegroundBrush"  Color="#4caf50"/>
    <SolidColorBrush x:Key="SummaryPillOffBackgroundBrush" Color="#2a2a2e"/>
    <SolidColorBrush x:Key="SummaryPillOffForegroundBrush" Color="#444448"/>

    <!-- ── Delete / destructive ───────────────────────────────────────────── -->
    <SolidColorBrush x:Key="DeleteForegroundBrush"     Color="#e05050"/>
    <SolidColorBrush x:Key="DeleteBackgroundBrush"     Color="#e05050"/>
    <SolidColorBrush x:Key="DeleteHoverBackgroundBrush" Color="#cc3a3a"/>

    <!-- ── Dialog ─────────────────────────────────────────────────────────── -->
    <SolidColorBrush x:Key="DialogBackgroundBrush" Color="#2a2a2e"/>
    <SolidColorBrush x:Key="DialogBorderBrush"     Color="#3a3a3c"/>

    <!-- ── Drag and drop ──────────────────────────────────────────────────── -->
    <SolidColorBrush x:Key="DragInsertionLineBrush"      Color="#0a84ff"/>
    <SolidColorBrush x:Key="DragDropTargetBorderBrush"   Color="#0a84ff"/>
    <SolidColorBrush x:Key="DragDropTargetBackgroundBrush" Color="#0a2040"/>
    <SolidColorBrush x:Key="DragSourceDimBackgroundBrush"  Color="#111114"/>

    <!-- ── Scrollbar ──────────────────────────────────────────────────────── -->
    <SolidColorBrush x:Key="ScrollTrackBrush"        Color="#1c1c1e"/>
    <SolidColorBrush x:Key="ScrollThumbBrush"        Color="#3a3a3c"/>
    <SolidColorBrush x:Key="ScrollThumbHoverBrush"   Color="#4a4a4e"/>
    <SolidColorBrush x:Key="ScrollThumbPressedBrush" Color="#0a84ff"/>

    <!-- ── Empty state text ───────────────────────────────────────────────── -->
    <SolidColorBrush x:Key="EmptyStateBrush" Color="#a0a0a8"/>

    <!-- ── Transparent ────────────────────────────────────────────────────── -->
    <SolidColorBrush x:Key="TransparentBrush" Color="Transparent"/>

    <!-- ── Font sizes (base — scaled at runtime by ScaleConverter) ───────── -->
    <sys:Double x:Key="FontSizeSmall">11</sys:Double>
    <sys:Double x:Key="FontSizeBody">13</sys:Double>
    <sys:Double x:Key="FontSizeBodyLarge">14</sys:Double>
    <sys:Double x:Key="FontSizeTitle">15</sys:Double>
    <sys:Double x:Key="FontSizeLargeTitle">18</sys:Double>

    <!-- ── Base layout sizes (scaled at runtime by ScaleConverter) ────────── -->
    <sys:Double x:Key="BaseRowHeight">38</sys:Double>
    <sys:Double x:Key="BaseCardMinHeight">70</sys:Double>
    <sys:Double x:Key="BaseCardWidth">220</sys:Double>
    <sys:Double x:Key="TitleBarHeight">44</sys:Double>
    <sys:Double x:Key="SummaryBarHeight">28</sys:Double>

    <!-- ════════════════════════════════════════════════════════════════════
         GLOBAL SCROLLBAR STYLE
         Applied to all ScrollViewers automatically via implicit style.
         ════════════════════════════════════════════════════════════════════ -->

    <Style x:Key="ScrollBarThumbStyle" TargetType="Thumb">
        <Setter Property="Background" Value="{StaticResource ScrollThumbBrush}"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Thumb">
                    <Border x:Name="ThumbBorder"
                            Background="{TemplateBinding Background}"
                            CornerRadius="3"/>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="ThumbBorder" Property="Background"
                                    Value="{StaticResource ScrollThumbHoverBrush}"/>
                        </Trigger>
                        <Trigger Property="IsDragging" Value="True">
                            <Setter TargetName="ThumbBorder" Property="Background"
                                    Value="{StaticResource ScrollThumbPressedBrush}"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="ScrollBarStyle" TargetType="ScrollBar">
        <Setter Property="Background" Value="{StaticResource ScrollTrackBrush}"/>
        <Setter Property="Width" Value="8"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ScrollBar">
                    <Grid Background="{TemplateBinding Background}">
                        <Track x:Name="PART_Track" IsDirectionReversed="True">
                            <Track.Thumb>
                                <Thumb Style="{StaticResource ScrollBarThumbStyle}"
                                       Margin="1,2"/>
                            </Track.Thumb>
                        </Track>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="ScrollBar" BasedOn="{StaticResource ScrollBarStyle}"/>

    <Style TargetType="ScrollViewer">
        <Setter Property="VerticalScrollBarVisibility" Value="Auto"/>
        <Setter Property="HorizontalScrollBarVisibility" Value="Disabled"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ScrollViewer">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="*"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>
                        <ScrollContentPresenter Grid.Column="0" Grid.Row="0"/>
                        <ScrollBar x:Name="PART_VerticalScrollBar"
                                   Grid.Column="1" Grid.Row="0"
                                   Orientation="Vertical"
                                   Value="{TemplateBinding VerticalOffset}"
                                   Maximum="{TemplateBinding ScrollableHeight}"
                                   ViewportSize="{TemplateBinding ViewportHeight}"
                                   Visibility="{TemplateBinding ComputedVerticalScrollBarVisibility}"/>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

</ResourceDictionary>
