Run an AUTO close-out.

Read Documents\AI PM\AI_Project_Manager.md in full and follow its close-out
procedure (§7), AUTO mode: full scope with the interactive gates removed —
no pre-flight pause, no plan approval; make the judgment calls and flag
them at the top of the report and in the commit message. Unanswered
questions are NEVER guessed into docs — they ride the handoff as open
items. Use the same stage skeletons and check bands as FULL mode; the
gates are what AUTO removes, not the presentation.

Self-review the git diff before committing, as in FULL mode. Write the
handoff to Documents\Handoff.md (overwrite) — opener and transient notes
only. End in a single commit whose message leads with the project version.

Note: AUTO close-out is an AI PM mode; it is unrelated to Claude Code's
"Auto" permission mode.
